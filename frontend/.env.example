# Leave blank for local dev — Vite's dev server proxies /api to the local backend (see vite.config.js).
# In production (e.g. Netlify), set this to your deployed backend's URL, no trailing slash:
#   VITE_API_URL=https://spark-tms-api.onrender.com
VITE_API_URL=
